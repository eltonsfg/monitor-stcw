app = "monitor-stcw-br"
primary_region = "gru"

[build]

[env]
  TZ = "UTC"

# Processo único — cron daemon (sem HTTP)
[processes]
  app = "cron -f"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
