# fly.toml — Monitor STCW (Fly.io São Paulo/GRU)

app = 'monitor-stcw-br'
primary_region = 'gru'

[build]

[env]
  TZ = 'UTC'

[processes]
  app = 'cron -f'

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
